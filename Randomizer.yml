info:
  name: Randomizer
  type: http
  seq: 1

http:
  method: GET
  url: https://postman-echo.com/get
  body:
    type: json
    data: |-
      //{
        //"Statement": "{{statementVar}}"
      //}
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const requests = ["Get Cat", "Get Dog"];
        const chosen = requests[Math.floor(Math.random() * requests.length)];
        console.log("Randomly selected: " + chosen);
        bru.runner.setNextRequest(chosen);
        //const tokenValue = 'tokenRuntime';
        //bru.setVar( 'statementVar', tokenValue )

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
